#include <iostream>
#include <fstream>
#include <string>

#include "graph.h"
#include <queue>
using namespace std;




int main() {
    











	return 0;
}
/* Map* map = new Map;
   ifstream fp;
   fp.open("graf1.txt");
   int n, m;
   fp >> n >> m;
   Graph* graph = new Graph();
   for (int i = 0; i < n; i++)
   {
       string name;
       fp >> name;
       graph->addVertex(name);

       //cout << list_of_vertexes[i] << endl;
   }
   string first, second;
   double distance;

   for (int i = 0; i < 3; i++) {
       fp >> first >> second >> distance;
       graph->addEdge(first, second, distance);
   }
   int k;
   cout << "unesi k";
   cin >> k;
   string wanted_vertex;
   cin >> wanted_vertex;



   delete graph; // proveri da li su ti dobri destruktori!!!
   */